What Is CSS ?
It is a language that is used to describe the style of a document 

CSS Basic Format:
h1{
   color:red;
}

How to Include CSS in html:
   1. Inline Css -> at large scale we dont use inline css
   2. Header: using Style Tag: -> For Multiple Pages This Is Not Good>
   3. External CSS -> link style name sheet. once linked all properties are included

color : used to set the color

BackGroundColor: Used to Set the BackGround Color  

Color System: Each Browser recognizes around 140 named colors.
   RGB(0,0,0), #Hex

   decimal -> binary -> hexadecimal

Text Properties:
text-align: --> based on parent only
   left,right,center justify
font-weight:
   bold, normal
text-decoration
   underline
line-heigt:
   space between two lines. normal, 2.5
letter-spaceing
   space between letter. noramal, 10px
font-size:
   1 inch : 96px
   pt,pc,cm,mm,in. --> absoulute (fixed)
   %, em,rem,ch,vh,vw..etc -> relative. variable

   font-family.
   
